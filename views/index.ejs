<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="styles/main.css">
    <title>Document</title>
</head>
<body>
    <main class="qr__content">
        <div>
            <% if (locals.qrImage) { %>
                <h1 class="title">Your QR</h1>
                <img src='<%= locals.qrImage %>' alt="">
            <% } else { %>
                <h1 class="title">Enter something to generate your qr-image</h1>
            <% } %>
        </div>
        <form action="/qr-code" method="post" class="">
            <label for="qrCodeContent">
                <input type="text" name="qrCodeContent" id="qrCodeContent" required>
            </label>
            <input type="submit" value="Generate QR">
        </form>
    </main>
</body>
</html>